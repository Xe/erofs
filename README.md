# erofs

An `fs.FS` driver for [erofs](https://erofs.docs.kernel.org/en/latest/) images.

This was created using the [Malus](https://malus.sh) technique: a team of AI agents looked over the erofs filesystem code and documentation to produce a specification. This specification was fed to a different AI agent team to implement it.
